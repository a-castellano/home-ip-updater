image: harbor.windmaker.net/limani/base_golang_1_26

stages:
  - lint
  - unit_tests
  - integration_tests
  - coverage
  - report
  - sonarqube-check
  - sonarqube-vulnerability-report
  - deploy
  - build

.install_mod: &install_mod
  - mkdir -p /root/go/pkg
  - mv mod /root/go/pkg/

lint_code:
  stage: lint
  rules:
    - when: always
  before_script:
    - export CC=/usr/bin/clang
  script:
    - make lint
    - ls -l /root/go/pkg/mod/
    - mv /root/go/pkg/mod ./
  artifacts:
    name: "lint-$CI_PIPELINE_ID"
    paths:
      - mod
    expire_in: 1 hour

unit_tests:
  stage: unit_tests
  before_script:
    - export CC=/usr/bin/clang
    - *install_mod
  script:
    - make test

integration_tests:
  stage: integration_tests
  before_script:
    - export CC=/usr/bin/clang
    - echo "$GIT_CRYPT_KEY_B64" | base64 -d | git-crypt unlock -
    - *install_mod
  script:
    - make test_integration

coverage:
  stage: coverage
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: $CI_COMMIT_BRANCH == 'master'
    - if: $CI_COMMIT_BRANCH == 'main'
    - if: $CI_COMMIT_BRANCH == 'develop'
    - if: $CI_COMMIT_BRANCH =~ '/^.*all.*$/'
  before_script:
    - export CC=/usr/bin/clang
    - echo "$GIT_CRYPT_KEY_B64" | base64 -d | git-crypt unlock -
    - *install_mod
  script:
    - make coverage
    - mv /root/go/pkg/mod ./
  coverage: /total:\s+.+\s(\d+\.\d+?)%$/
  artifacts:
    name: "coverage-$CI_PIPELINE_ID"
    paths:
      - cover
      - mod
    expire_in: 1 hour

code_coverage_report:
  stage: report
  before_script:
    - export CC=/usr/bin/clang
    - *install_mod
  script:
    - make coverhtml
  rules:
    - if: $CI_COMMIT_BRANCH == 'master'
    - if: $CI_COMMIT_BRANCH == 'main'
    - if: $CI_COMMIT_BRANCH =~ '/^.*all.*$/'
  dependencies:
    - coverage
  artifacts:
    name: "coverage-$CI_PIPELINE_ID"
    paths:
      - coverage.html
      - cover
    expire_in: 1 hour

sonarqube-check:
  stage: sonarqube-check
  image:
    name: sonarsource/sonar-scanner-cli:11
    entrypoint: [""]
  variables:
    SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar" # Defines the location of the analysis task cache
    GIT_DEPTH: "0" # Tells git to fetch all the branches of the project, required by the analysis task
  script:
    - sonar-scanner -Dproject.settings=development/sonar-project.properties -Dsonar.host.url="${SONAR_HOST_URL}"
  allow_failure: true
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: $CI_COMMIT_BRANCH == 'master'
    - if: $CI_COMMIT_BRANCH == 'main'
    - if: $CI_COMMIT_BRANCH == 'develop'
    - if: $CI_COMMIT_BRANCH =~ '/^.*all.*$/'
  dependencies:
    - coverage

sonarqube-vulnerability-report:
  stage: sonarqube-vulnerability-report
  image:
    name: sonarsource/sonar-scanner-cli:11
    entrypoint: [""]
  variables:
    SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar" # Defines the location of the analysis task cache
    GIT_DEPTH: "0" # Tells git to fetch all the branches of the project, required by the analysis task
  script:
    - 'curl -u "${SONAR_TOKEN}:" "${SONAR_HOST_URL}/api/issues/gitlab_sast_export?projectKey=a-castellano_home-ip-updater_533a7009-26fb-43b9-b6f3-eb5326c083b6&branch=${CI_COMMIT_BRANCH}&pullRequest=${CI_MERGE_REQUEST_IID}" -o gl-sast-sonar-report.json'
  allow_failure: true
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: $CI_COMMIT_BRANCH == 'master'
    - if: $CI_COMMIT_BRANCH == 'main'
    - if: $CI_COMMIT_BRANCH == 'develop'
    - if: $CI_COMMIT_BRANCH =~ '/^.*all.*$/'
  artifacts:
    expire_in: 1 day
    reports:
      sast: gl-sast-sonar-report.json
  dependencies:
    - coverage

pages:
  stage: deploy
  image: harbor.windmaker.net/limani/base
  script:
    - mkdir -p public
    - cp coverage.html public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == 'master'
    - if: $CI_COMMIT_BRANCH == 'main'
    - if: $CI_COMMIT_BRANCH =~ '/^.*all.*$/'
  dependencies:
    - code_coverage_report

build_package:
  stage: build
  before_script:
    - export CC=/usr/bin/clang
    - export GOOS=linux
    - export GOARCH=arm64
  script:
    - make build
    - cd packaging/nfpm && nfpm package --packager archlinux && cd ../..
    - mkdir arch_package
    - mv packaging/nfpm/*.pkg.tar.zst arch_package/
  dependencies:
    - coverage
  artifacts:
    name: "coverage-$CI_PIPELINE_ID"
    paths:
      - arch_package
    expire_in: 1 hour
  rules:
    - if: $CI_COMMIT_BRANCH == 'master'
    - if: $CI_COMMIT_BRANCH == 'main'
    - if: $CI_COMMIT_BRANCH =~ '/^.*all.*$/'
